# Server Configuration
PORT=5000
NODE_ENV=development

# Firebase Admin SDK (OPTIONAL - App works without Firebase using localStorage)
# Option 1: Use environment variables
# FIREBASE_PROJECT_ID=your_project_id
# FIREBASE_CLIENT_EMAIL=your_client_email@your_project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour\nPrivate\nKey\nHere\n-----END PRIVATE KEY-----\n"

# Option 2: Use service account key file (Recommended)
# Download serviceAccountKey.json from Firebase Console
# Place it in backend/ directory and uncomment:
# FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json

# Note: Firebase is optional. If not configured, the app will:
# - Use localStorage in frontend for data persistence
# - Skip Firestore operations
# - Disable Firebase authentication

# Groq API (for AI responses - FREE and FAST!)
GROQ_API_KEY=your_groq_api_key_here

# Get your FREE Groq API key at: https://console.groq.com/keys
